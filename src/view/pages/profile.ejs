<!DOCTYPE html>
<html lang="en" dir="ltr">
<head>
	<meta charset="UTF-8">
	<meta name="viewport" content="width=device-width, initial-scale=1.0">
	<title><%= title %></title>
	<script src="https://cdn.tailwindcss.com"></script>
	<link rel="preconnect" href="https://fonts.googleapis.com">
	<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
	<link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap" rel="stylesheet">
	<style>
		body { font-family: Inter, system-ui, -apple-system, Segoe UI, Roboto, Arial; }
	</style>
</head>
<body class="bg-slate-50 text-slate-900">
	<header class="border-b bg-white">
		<nav class="max-w-6xl mx-auto px-4 h-16 flex items-center justify-between">
			<a href="/" class="font-semibold text-slate-900">FinTrack</a>
			<div class="flex items-center gap-3">
				<a href="/" class="text-sm <%= (typeof path !== 'undefined' && path === '/') ? 'text-slate-900 font-medium' : 'text-slate-600 hover:text-slate-900' %>">Dashboard</a>
				<a href="/transactions" class="text-sm <%= (typeof path !== 'undefined' && path.startsWith('/transactions')) ? 'text-slate-900 font-medium' : 'text-slate-600 hover:text-slate-900' %>">Transactions</a>
				<a href="/categories" class="text-sm <%= (typeof path !== 'undefined' && path.startsWith('/categories')) ? 'text-slate-900 font-medium' : 'text-slate-600 hover:text-slate-900' %>">Categories</a>
				<a href="/budgets" class="text-sm <%= (typeof path !== 'undefined' && path.startsWith('/budgets')) ? 'text-slate-900 font-medium' : 'text-slate-600 hover:text-slate-900' %>">Budgets</a>
				<a href="/goals" class="text-sm <%= (typeof path !== 'undefined' && path.startsWith('/goals')) ? 'text-slate-900 font-medium' : 'text-slate-600 hover:text-slate-900' %>">Saving Goals</a>
				<a href="/profile" class="text-sm <%= (typeof path !== 'undefined' && path.startsWith('/profile')) ? 'text-slate-900 font-medium' : 'text-slate-600 hover:text-slate-900' %>">Profile</a>
				<% if (!emailVerified) { %>
					<a href="/verify" class="text-sm text-amber-600 hover:text-amber-700">Verify Email</a>
				<% } %>
				<form action="/logout" method="post">
					<button type="submit" class="px-3 py-1.5 rounded-md bg-slate-900 text-white hover:bg-slate-800 text-sm">Logout</button>
				</form>
			</div>
		</nav>
	</header>
	<main class="max-w-6xl mx-auto px-4 py-10">
		<% if (typeof success !== 'undefined' && success) { %>
			<div class="mb-6 p-4 bg-green-50 border border-green-200 rounded-md">
				<div class="flex">
					<div class="flex-shrink-0">
						<svg class="h-5 w-5 text-green-400" viewBox="0 0 20 20" fill="currentColor">
							<path fill-rule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zm3.707-9.293a1 1 0 00-1.414-1.414L9 10.586 7.707 9.293a1 1 0 00-1.414 1.414l2 2a1 1 0 001.414 0l4-4z" clip-rule="evenodd" />
						</svg>
					</div>
					<div class="ml-3">
						<p class="text-sm font-medium text-green-800"><%= success %></p>
					</div>
				</div>
			</div>
		<% } %>

		<% if (typeof validationError !== 'undefined' && validationError) { %>
			<div class="mb-6 p-4 bg-red-50 border border-red-200 rounded-md">
				<div class="flex">
					<div class="flex-shrink-0">
						<svg class="h-5 w-5 text-red-400" viewBox="0 0 20 20" fill="currentColor">
							<path fill-rule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zM8.707 7.293a1 1 0 00-1.414 1.414L8.586 10l-1.293 1.293a1 1 0 101.414 1.414L10 11.414l1.293 1.293a1 1 0 001.414-1.414L11.414 10l1.293-1.293a1 1 0 00-1.414-1.414L10 8.586 8.707 7.293z" clip-rule="evenodd" />
						</svg>
					</div>
					<div class="ml-3">
						<h3 class="text-sm font-medium text-red-800">Please correct the following errors:</h3>
						<div class="mt-2 text-sm text-red-700">
							<% if (typeof validationErrors !== 'undefined') { %>
								<ul class="list-disc list-inside space-y-1">
									<% Object.keys(validationErrors).forEach(function(field) { %>
										<li><%= validationErrors[field] %></li>
									<% }); %>
								</ul>
							<% } else { %>
								<p><%= validationError %></p>
							<% } %>
						</div>
					</div>
				</div>
			</div>
		<% } %>

		<% if (typeof error !== 'undefined' && error) { %>
			<div class="mb-6 p-4 bg-red-50 border border-red-200 rounded-md">
				<div class="flex">
					<div class="flex-shrink-0">
						<svg class="h-5 w-5 text-red-400" viewBox="0 0 20 20" fill="currentColor">
							<path fill-rule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zM8.707 7.293a1 1 0 00-1.414 1.414L8.586 10l-1.293 1.293a1 1 0 101.414 1.414L10 11.414l1.293 1.293a1 1 0 001.414-1.414L11.414 10l1.293-1.293a1 1 0 00-1.414-1.414L10 8.586 8.707 7.293z" clip-rule="evenodd" />
						</svg>
					</div>
					<div class="ml-3">
						<p class="text-sm font-medium text-red-800"><%= error %></p>
					</div>
				</div>
			</div>
		<% } %>

		<section class="grid grid-cols-1 md:grid-cols-3 gap-6">
			<div class="md:col-span-1 bg-white border rounded-xl p-6">
				<div class="flex items-center gap-4">
					<div class="w-20 h-20 rounded-full bg-slate-100 flex items-center justify-center border">
						<span class="text-2xl font-semibold text-slate-700"><%= (user && user.name && user.name[0]) ? user.name[0].toUpperCase() : 'U' %></span>
					</div>
					<div>
						<div class="text-lg font-semibold"><%= user && user.name %></div>
						<div class="text-sm text-slate-600"><%= user && user.email %></div>
						<div class="mt-2 inline-block px-2 py-1 rounded-full text-xs border <%= emailVerified ? 'text-green-700 border-green-300 bg-green-50' : 'text-amber-700 border-amber-300 bg-amber-50' %>"><%= emailVerified ? 'Email verified' : 'Not verified' %></div>
					</div>
				</div>
			</div>
			<div class="md:col-span-2 bg-white border rounded-xl p-6">
				<h3 class="text-lg font-semibold mb-4">Profile details</h3>
				
				<!-- Name Update Form -->
				<form action="/profile" method="POST" class="mb-6">
					<div class="flex items-end gap-3">
						<div class="flex-1">
							<label for="name" class="block text-sm font-medium text-slate-700 mb-2">Full Name</label>
							<input 
								type="text" 
								id="name" 
								name="name" 
								value="<%= (typeof oldInput !== 'undefined' && oldInput.name) ? oldInput.name : (user && user.name) %>"
								required
								minlength="3"
								maxlength="100"
								class="w-full px-3 py-2 border <%= (typeof validationErrors !== 'undefined' && validationErrors.name) ? 'border-red-300' : 'border-slate-300' %> rounded-md shadow-sm focus:outline-none focus:ring-2 focus:ring-blue-500 focus:border-blue-500"
							>
							<% if (typeof validationErrors !== 'undefined' && validationErrors.name) { %>
								<p class="mt-1 text-sm text-red-600"><%= validationErrors.name %></p>
							<% } %>
						</div>
						<button 
							type="submit" 
							class="px-4 py-2 bg-blue-600 text-white rounded-md hover:bg-blue-700 focus:outline-none focus:ring-2 focus:ring-blue-500 focus:ring-offset-2 font-medium"
						>
							Update Name
						</button>
					</div>
				</form>

				<!-- Read-only Information -->
				<div class="space-y-3 border-t pt-4">
					<div>
						<div class="text-xs uppercase tracking-wide text-slate-500">Email</div>
						<div class="font-medium"><%= user && user.email %></div>
					</div>
					<div>
						<div class="text-xs uppercase tracking-wide text-slate-500">User ID</div>
						<div class="font-medium"><%= user && user.id %></div>
					</div>
					<div>
						<div class="text-xs uppercase tracking-wide text-slate-500">Account Status</div>
						<div class="font-medium">
							<span class="inline-flex items-center px-2.5 py-0.5 rounded-full text-xs font-medium <%= emailVerified ? 'bg-green-100 text-green-800' : 'bg-amber-100 text-amber-800' %>">
								<%= emailVerified ? 'Verified' : 'Pending Verification' %>
							</span>
						</div>
					</div>
				</div>
			</div>
		</section>
	</main>
	<footer class="border-t bg-white">
		<div class="max-w-6xl mx-auto px-4 py-6 text-sm text-slate-500 flex items-center justify-between">
			<p>&copy; <%= new Date().getFullYear() %> FinTrack. All rights reserved.</p>
			<div class="flex items-center gap-3">
				<a class="hover:text-slate-700" href="#">Privacy</a>
				<a class="hover:text-slate-700" href="#">Terms</a>
			</div>
		</div>
	</footer>
</body>
</html> 