<!DOCTYPE html>
<html lang="en" dir="ltr">
<head>
	<meta charset="UTF-8">
	<meta name="viewport" content="width=device-width, initial-scale=1.0">
	<title><%= title %></title>
	<script src="https://cdn.tailwindcss.com"></script>
	<link rel="preconnect" href="https://fonts.googleapis.com">
	<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
	<link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap" rel="stylesheet">
	<style>
		body { font-family: Inter, system-ui, -apple-system, Segoe UI, Roboto, Arial; }
	</style>
</head>
<body class="bg-slate-50 text-slate-900">
	<header class="border-b bg-white">
		<nav class="max-w-6xl mx-auto px-4 h-16 flex items-center justify-between">
			<a href="/" class="font-semibold text-slate-900">FinTrack</a>
			<div class="flex items-center gap-3">
				<% if (user) { %>
					<a href="/" class="text-sm text-slate-600 hover:text-slate-900">Dashboard</a>
					<form action="/logout" method="post" class="inline-block">
						<button class="text-sm text-slate-600 hover:text-slate-900">Logout</button>
					</form>
				<% } else { %>
					<a href="/login" class="text-sm text-slate-600 hover:text-slate-900">Sign in</a>
					<a href="/register" class="text-sm text-slate-600 hover:text-slate-900">Sign up</a>
				<% } %>
			</div>
		</nav>
	</header>
	<main class="max-w-6xl mx-auto px-4 py-10">
		<section class="max-w-md mx-auto bg-white border rounded-xl p-6 shadow-sm space-y-4">
			<h2 class="text-xl font-semibold">Verify your email</h2>
			<form action="/verify/send" method="post">
				<button type="submit" class="px-4 py-2 rounded-md bg-slate-900 text-white hover:bg-slate-800">Send verification code</button>
			</form>
			<% if (old && old.info) { %><p class="text-green-700 text-sm"><%= old.info %></p><% } %>

			<form action="/verify" method="post" class="space-y-3">
				<label class="block text-sm text-slate-600">Verification code</label>
				<input type="text" name="code" required class="mt-1 w-full rounded-md border-slate-300 focus:border-slate-400 focus:ring-slate-400" />
				<button type="submit" class="w-full px-4 py-2 rounded-md bg-slate-900 text-white hover:bg-slate-800">Verify</button>
			</form>

			<% if (errors && errors.general) { %><p class="text-red-600 text-sm"><%= errors.general %></p><% } %>
		</section>
	</main>
	<footer class="border-t bg-white">
		<div class="max-w-6xl mx-auto px-4 py-6 text-sm text-slate-500 flex items-center justify-between">
			<p>&copy; <%= new Date().getFullYear() %> FinTrack. All rights reserved.</p>
			<div class="flex items-center gap-3">
				<a class="hover:text-slate-700" href="#">Privacy</a>
				<a class="hover:text-slate-700" href="#">Terms</a>
			</div>
		</div>
	</footer>
</body>
</html> 