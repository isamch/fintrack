<!DOCTYPE html>
<html lang="ar" dir="rtl">
<head>
	<meta charset="UTF-8">
	<meta name="viewport" content="width=device-width, initial-scale=1.0">
	<title><%= title %></title>
	<style>
		body { font-family: Arial, sans-serif; margin: 2rem; }
		.container { max-width: 480px; margin: auto; }
		label { display:block; margin-top: 12px; }
		input { width: 100%; padding: 10px; margin-top: 6px; }
		button { margin-top: 16px; padding: 10px 16px; }
		.info { color: #0a7; margin-top: 8px; }
		.error { color: #b00020; margin-top: 8px; }
	</style>
</head>
<body>
	<div class="container">
		<h2>تفعيل البريد الإلكتروني</h2>
		<form action="/verify/send" method="post">
			<button type="submit">إرسال رمز التفعيل إلى البريد</button>
		</form>
		<% if (old && old.info) { %><div class="info"><%= old.info %></div><% } %>

		<form action="/verify" method="post">
			<label>رمز التفعيل</label>
			<input type="text" name="code" required />
			<button type="submit">تفعيل</button>
		</form>

		<% if (errors && errors.general) { %><div class="error"><%= errors.general %></div><% } %>
	</div>
</body>
</html> 