<!DOCTYPE html>
<html lang="en" dir="ltr">
<head>
	<meta charset="UTF-8">
	<meta name="viewport" content="width=device-width, initial-scale=1.0">
	<title><%= title %></title>
	<script src="https://cdn.tailwindcss.com"></script>
	<link rel="preconnect" href="https://fonts.googleapis.com">
	<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
	<link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap" rel="stylesheet">
	<style>
		body { font-family: Inter, system-ui, -apple-system, Segoe UI, Roboto, Arial; }
	</style>
</head>
<body class="bg-slate-50 text-slate-900">
	<header class="border-b bg-white">
		<nav class="max-w-6xl mx-auto px-4 h-16 flex items-center justify-between">
			<a href="/" class="font-semibold text-slate-900">FinTrack</a>
			<div class="flex items-center gap-3">
				<a href="/" class="text-sm <%= (typeof path !== 'undefined' && path === '/') ? 'text-slate-900 font-medium' : 'text-slate-600 hover:text-slate-900' %>">Dashboard</a>
				<a href="/transactions" class="text-sm <%= (typeof path !== 'undefined' && path.startsWith('/transactions')) ? 'text-slate-900 font-medium' : 'text-slate-600 hover:text-slate-900' %>">Transactions</a>
				<a href="/budgets" class="text-sm <%= (typeof path !== 'undefined' && path.startsWith('/budgets')) ? 'text-slate-900 font-medium' : 'text-slate-600 hover:text-slate-900' %>">Budgets</a>
				<a href="/goals" class="text-sm <%= (typeof path !== 'undefined' && path.startsWith('/goals')) ? 'text-slate-900 font-medium' : 'text-slate-600 hover:text-slate-900' %>">Saving Goals</a>
				<% if (user) { %>
					<a href="/profile" class="text-sm <%= (typeof path !== 'undefined' && path.startsWith('/profile')) ? 'text-slate-900 font-medium' : 'text-slate-600 hover:text-slate-900' %>">Profile</a>
					<% if (!emailVerified) { %>
						<a href="/verify" class="text-sm text-amber-600 hover:text-amber-700">Verify Email</a>
					<% } %>
					<form action="/logout" method="post" class="inline-block">
						<button class="text-sm text-slate-600 hover:text-slate-900">Logout</button>
					</form>
				<% } else { %>
					<a href="/login" class="text-sm text-slate-600 hover:text-slate-900">Login</a>
					<a href="/register" class="text-sm text-slate-600 hover:text-slate-900">Register</a>
				<% } %>
			</div>
		</nav>
	</header>
	<main class="max-w-6xl mx-auto px-4 py-10">

		<h1 class="text-2xl font-semibold mb-6">Dashboard — <%= month %>/<%= year %></h1>

		<div class="grid gap-4 md:grid-cols-2">
			<div class="bg-white border rounded-md p-4">
				<div class="text-sm text-slate-500">Total Expenses</div>
				<div class="text-2xl font-semibold text-slate-900 mt-1"><%= totalExpenses.toFixed(2) %></div>
			</div>
			<div class="bg-white border rounded-md p-4">
				<div class="text-sm text-slate-500">Total Income</div>
				<div class="text-2xl font-semibold text-slate-900 mt-1"><%= totalIncome.toFixed(2) %></div>
			</div>
			<div class="bg-white border rounded-md p-4 md:col-span-2">
				<div class="flex items-center justify-between">
					<div>
						<div class="text-sm text-slate-500">Budget Utilization</div>
						<div class="text-slate-900 font-medium"><%= totalBudgetSpent.toFixed(2) %> / <%= totalBudget.toFixed(2) %></div>
					</div>
					<div class="text-sm text-slate-600"><%= budgetPct %>%</div>
				</div>
				<div class="mt-2 w-full h-2 bg-slate-200 rounded-full overflow-hidden">
					<div class="h-2 bg-amber-500 <%= widthClass(budgetPct) %>"></div>
				</div>
			</div>
			<div class="bg-white border rounded-md p-4 md:col-span-2">
				<div class="flex items-center justify-between">
					<div>
						<div class="text-sm text-slate-500">Saving Goals Progress</div>
						<div class="text-slate-900 font-medium"><%= sumCurrent.toFixed(2) %> / <%= sumTarget.toFixed(2) %></div>
					</div>
					<div class="text-sm text-slate-600"><%= goalsPct %>%</div>
				</div>
				<div class="mt-2 w-full h-2 bg-slate-200 rounded-full overflow-hidden">
					<div class="h-2 bg-blue-500 <%= widthClass(goalsPct) %>"></div>
				</div>
			</div>
		</div>

		<% if (approachingDue > 0) { %>
			<div class="mt-6 p-4 border rounded-md bg-red-50 text-red-700 text-sm">
				You have <%= approachingDue %> goal(s) with due date within 7 days. Check <a href="/goals" class="underline">Saving Goals</a>.
			</div>
		<% } %>

	</main>
	<footer class="border-t bg-white">
		<div class="max-w-6xl mx-auto px-4 py-6 text-sm text-slate-500 flex items-center justify-between">
			<span>FinTrack</span>
			<span>&copy; <%= new Date().getFullYear() %></span>
		</div>
	</footer>
</body>
</html> 