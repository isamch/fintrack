<!DOCTYPE html>
<html lang="ar" dir="rtl">
<head>
	<meta charset="UTF-8">
	<meta name="viewport" content="width=device-width, initial-scale=1.0">
	<title><%= title %></title>
	<style>
		body { font-family: Arial, sans-serif; margin: 2rem; }
		.container { max-width: 480px; margin: auto; }
		label { display:block; margin-top: 12px; }
		input { width: 100%; padding: 10px; margin-top: 6px; }
		button { margin-top: 16px; padding: 10px 16px; }
		.error { color: #b00020; margin-top: 8px; }
	</style>
</head>
<body>
	<div class="container">
		<h2>تسجيل حساب جديد</h2>
		<form action="/auth/register" method="post">
			<label>الاسم الكامل</label>
			<input type="text" name="name" value="<%= (old && old.name) || '' %>" required />
			<% if (errors && errors.name) { %><div class="error"><%= errors.name %></div><% } %>

			<label>البريد الإلكتروني</label>
			<input type="email" name="email" value="<%= (old && old.email) || '' %>" required />
			<% if (errors && errors.email) { %><div class="error"><%= errors.email %></div><% } %>

			<label>كلمة المرور</label>
			<input type="password" name="password" required />
			<% if (errors && errors.password) { %><div class="error"><%= errors.password %></div><% } %>

			<% if (errors && errors.general) { %><div class="error"><%= errors.general %></div><% } %>
			<button type="submit">تسجيل</button>
		</form>
		<p>لديك حساب؟ <a href="/auth/login">تسجيل الدخول</a></p>
	</div>
</body>
</html> 